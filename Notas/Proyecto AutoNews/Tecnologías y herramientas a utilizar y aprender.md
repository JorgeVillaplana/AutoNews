
### Python

*   `FastAPI` → tu backend principal (rápido, moderno, async)
*   `feedparser` → leer RSS/Atom
*   `BeautifulSoup` → parsear HTML
*   `imaplib` → acceder a un email via IMAP
*   `sqlalchemy` → ORM para PostgreSQL
*   `APScheduler` → tareas programadas (cron jobs)
*   `pydantic` → validación de datos
*   `requests` → llamadas HTTP
    
      
    Opcional avanzado:
*   `playwright` → scraping con JS
*   `uvicorn / gunicorn` → despliegue

### PostgreSQL

*   Full-text search (FTS)
*   JSONB (para guardar información flexible)
*   Cómo hacer consultas eficientes

### Procesado y ML

*   OpenAI embeddings (`text-embedding-3-large`)
*   Similaridad coseno (`numpy`, `sklearn`)
*   LLM para clasificación (no hace falta entrenar nada)
*   Almacenamiento de vectores en DB (puedes usar `pgvector`)

### Texto a voz

*   OpenAI TTS

### Telegram Bot

*   python-telegram-bot

### Otras Herramientas:

*   Markdown en tu repo
*   Diagramas con Excalidraw o Mermaid
*   Documentación auto-generada con FastAPI